import React from 'react';
import style from './header.module.scss';

const Header = () => (
  <div className={style.wrapper}>
    <h1 className={style.title}>FIFA Blog</h1>
    <nav></nav>
  </div>
);
export default Header;
